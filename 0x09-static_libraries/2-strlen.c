#include "main.h"
#include <string.h>

/**
 * _strlen - Entry point
 * @s:A pointer input to a string
 * Description:Return the length of a string
 * Return:Length of the string
 */
int _strlen(char *s)
{
	int l;

	l = strlen(s);
	return (l);
}
